apiVersion: v1
kind: Service
metadata:
  name: user-service-svc
  labels:
    app: user-service-app
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30000
      protocol: TCP
      name: app
  selector:
    app: dockerpoc
